<!DOCTYPE html>
<html lang="es">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ODS 7 - Energía asequible y no contaminante</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/main.css">
  <style>
    .dashboard {
      display: flex;
      flex-direction: column;
      gap: 1rem;
      padding: 1rem;
    }

    .top-section {
      background-color: #e74c3c;
      /* rojo */
      height: 400px;
      border-radius: 10px;
      padding: 1rem;
      color: white;
      font-weight: bold;
    }

    .bottom-section {
      display: flex;
      gap: 1rem;
    }

    .left-box,
    .right-box {
      flex: 1;
      height: 250px;
      border-radius: 10px;
      padding: 1rem;
      color: white;
      font-weight: bold;
    }

    .left-box {
      background-color: #3498db;
      /* azul */
    }

    .right-box {
      background-color: #2ecc71;
      /* verde */
    }

    img {
      width: 100%;
      height: 100%;
      object-fit: scale-down;
      border-radius: 10px;
    }

    .img-link {
      display: block;
      margin: 1.5rem auto 0 auto;
      text-align: center;
    }
    main {
      padding: 2rem 1rem;
      max-width: 1100px;
      margin: auto;
      animation: fadeInUp 1s ease-out;
    }
    /* Dashboard interactivo */
    .dashboard-interactivo {
      display: flex;
      flex-wrap: wrap;
      gap: 2rem;
      justify-content: center;
      margin: 2rem 0;
    }
    .dashboard-card {
      flex: 1 1 350px;
      min-width: 300px;
      max-width: 500px;
      background: #fff;
      border-radius: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.08);
      padding: 1.5rem;
      margin-bottom: 1rem;
    }
    @media (max-width: 900px) {
      .bottom-section, .dashboard-interactivo {
        flex-direction: column;
        align-items: center;
      }
      .dashboard-card {
        max-width: 95vw;
      }
    }
  </style>
</head>

<body>
  <header>
    <h1>Objetivo de Desarrollo Sostenible 7</h1>
    <h2>Energía asequible y no contaminante</h2>
  </header>

  <main>
    <section class="dashboard">
      <div class="top-section">
        <h3>ODS 7: Energía asequible y no contaminante</h3>
        <p>El ODS 7 busca garantizar el acceso universal a energía asequible, fiable, sostenible y moderna para todos.</p>
      </div>
      <div class="bottom-section">
        <div class="left-box">
          <h4>Datos de la comarca</h4>
          <p>Potencia instalada: 1.200 MW</p>
          <p>Consumo real: 1.000 MW</p>
          <p>Porcentaje de autosuficiencia: 83%</p>
        </div>
        <div class="right-box">
          <h4>Comparativa con otras comarcas</h4>
          <p>Comarca A: Autosuficiente</p>
          <p>Comarca B: Deficitarias</p>
          <p>Comarca C: Excedentarias</p>
        </div>
      </div>

    <!-- Dashboard interactivo -->
    <section>
      <h3 style="text-align:center; margin-top:2rem;">📊 Dashboard interactivo: Comparativa energética</h3>
      <div class="dashboard-interactivo">
        <div class="dashboard-card">
          <h4 style="text-align:center;">Potencia instalada vs Consumo real</h4>
          <canvas id="barChart"></canvas>
        </div>
        <div class="dashboard-card" style="max-width:350px;">
          <h4 style="text-align:center;">Distribución de comarcas</h4>
          <canvas id="pieChart"></canvas>
        </div>
      </div>
    </section>
  </main>

  <div style="text-align:center; margin: 2rem 0;">
    <a href="/home" class="back-home-btn">
      ⚡ Volver a la página principal
    </a>
  </div>

  <!-- Scripts del dashboard interactivo -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  <script>
    // Datos de ejemplo, reemplaza por los datos reales de tu análisis
    const comarcas = ['Barcelona', 'Vallès Occ.', 'Terra Alta', 'Alt Empordà', 'Baix Llobregat'];
    const potenciaInstalada = [200, 150, 400, 350, 100]; // MW
    const consumoReal = [350, 300, 100, 120, 250]; // MW

    // Gráfico de barras
    new Chart(document.getElementById('barChart'), {
      type: 'bar',
      data: {
        labels: comarcas,
        datasets: [
          {
            label: 'Potencia instalada (MW)',
            data: potenciaInstalada,
            backgroundColor: 'rgba(0, 123, 255, 0.7)'
          },
          {
            label: 'Consumo real (MW)',
            data: consumoReal,
            backgroundColor: 'rgba(40, 167, 69, 0.7)'
          }
        ]
      },
      options: {
        responsive: true,
        plugins: {
          legend: { position: 'top' },
          title: { display: false }
        },
        scales: {
          y: { beginAtZero: true }
        }
      }
    });

    // Datos de ejemplo para el gráfico de sectores
    const autosuficientes = 2;
    const deficitarias = 2;
    const excedentarias = 1;

    new Chart(document.getElementById('pieChart'), {
      type: 'doughnut',
      data: {
        labels: ['Autosuficientes', 'Deficitarias', 'Excedentarias'],
        datasets: [{
          data: [autosuficientes, deficitarias, excedentarias],
          backgroundColor: [
            'rgba(40, 167, 69, 0.7)',
            'rgba(220, 53, 69, 0.7)',
            'rgba(255, 193, 7, 0.7)'
          ]
        }]
      },
      options: {
        responsive: true,
        plugins: {
          legend: { position: 'bottom' }
        }
      }
    });
  </script>

  <%- include('partials/footer', { footerText: footerText }) %>
</body>
</html>